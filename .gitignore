# --- Python ---*.py[cod]*.egg*.egg-info/dist/build/__pycache__/*.so*.log# Virtual environmentsvenv/env/ENV/.venv/# Configurações de ambiente.env.env.*.local# --- Flask / Web ---instance/*.db*.sqlite3# Arquivos estáticos gerados / uploads temporáriosuploads/tmp/*.cache# --- Git LFS pointers (não ignorar .mp4, pois já estão sendo gerenciados pelo LFS) ---# se houvesse outros tipos grandes que você não quer versionar fora do LFS:# *.mp4# --- Node (caso use ferramentas JS/Front-end) ---node_modules/npm-debug.log*yarn-error.log*yarn-debug.log*# --- IDEs e editores ---.vscode/.idea/*.sublime-project*.sublime-workspace# --- Sistema operacional ---.DS_StoreThumbs.dbdesktop.ini